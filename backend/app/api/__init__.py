"""API (router) layer: HTTP validation, serialization, and dependency injection.

Routers contain no business logic, database queries, or AI calls - they
delegate to the service layer and return standardized responses.
"""
